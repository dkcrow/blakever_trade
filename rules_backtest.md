# QClaw Rules - 回测框架规范

## 回测框架统一规则

### 1. 回测框架版本
- **必须使用 BlakeVer 最新版本**进行所有回测任务
- 当前最新版本：`blakever_regime_backtest_v4.py`
- 框架类型：**VectorBT**（不是Backtrader，也不是自定义框架）
- 路径：`C:\Users\blakehao\.qclaw\workspace\blakever_regime_backtest_v4.py`

### 1.1 禁止使用自定义框架
- ❌ 禁止在 `strategy_arena/` 下创建新的自定义回测脚本
- ❌ 禁止使用 `backtest_history*.py` 类型的自定义框架
- ✅ 所有回测必须基于 VectorBT 的 BlakeVer v3/v4

### 2. 止损逻辑规范（所有回测脚本必须遵守）
- **止损判断**：用**最低价（low）**判断，不是收盘价
- **止损触发**：盘中价格 ≤ 止损线（entry × 0.92）即触发
- **止损卖出价**：按**止损线价格**卖出，不是收盘价
- **回测与实盘一致**：回测止损逻辑必须和`拉普拉斯_盘中监控_最终版.py`完全一致

### 3. 定时任务更新
- **拉普拉斯盘中监控**：已更新为 `拉普拉斯_盘中监控_v14_最终修复版.py`
- 定时任务ID：`qixing-sanma-intraday-monitor`
- 邮件排版：**三马七星同款排版+亮色主题**

### 3.1 邮件排版规范（强制）
**布局结构**（1:1 复制三马七星）：
- 头部：居中，底部2px灰线，标题20px + 副标题12px
- 前三名栏：白色圆角框+边框，14px加粗，用数字1/2/3替代🥇🥈🥉（避免GBK编码错误）
- 排名变动：浅灰背景圆角框，13px
- Top10表格：白色圆角框+边框，表头11px + 数据12-15px
- 交易记录：含理由列+ETF代码，当前交易标黄
- 说明区：浅灰背景圆角框，11px
- 页脚：居中灰色，10px

**颜色方案**（亮色版）：
| 元素 | 颜色 | 说明 |
|------|------|------|
| 背景 | `#f8f9fa` 浅灰 | 护眼不刺眼 |
| 卡片 | `#ffffff` 纯白 | 内容区域 |
| 主文字 | `#111827` 近黑 | 最高对比度 |
| 次要文字 | `#6b7280` 灰色 | 说明文字 |
| ETF代码 | `#1d4ed8` 蓝色 | 突出代码 |
| 正动量 | `#059669` 深绿 | 得分>0.1 |
| 负动量 | `#dc2626` 深红 | 得分<0 |
| 价格 | `#059669` 深绿 | 15px加粗 |
| 当前交易 | `#fef3c7` 浅黄 | 整行背景 |

**交易记录表格列**：
1. 日期 | 2. ETF（代码）| 3. 操作 | 4. 价格 | 5. 盈亏% | 6. 理由

**强制要求**：
- ✅ 字体大小必须严格按三马七星（20/14/13/12/11/10px）
- ✅ 必须包含近20次交易记录（带理由列）
- ✅ 当次定时任务触发的交易整行标黄（浅黄色#fef3c7）
- ✅ ETF列格式：`ETF名称 (代码)` 如 `黄金ETF华安 (518880)`
- ✅ **禁止emoji**（避免PowerShell GBK编码错误，用数字1/2/3替代🥇🥈🥉）
- ❌ 禁止深色背景（必须用亮色主题）

### 3.2 动量得分计算公式（关键！）
**正确的年化动量计算**：
```python
# polyfit返回的斜率slope = 每交易日对数收益率
# 年化 = slope × 250 （直接相乘，不要exp()！）

# 短期25日动量
slope, _ = np.polyfit(x_short, np.log(prices[-26:]), 1, w=weights)
short_annual = slope * 250  # ✅ 正确
# short_annual = (np.exp(slope * 250) - 1)  # ❌ 错误！会导致23695%这种荒谬值

# 长期250日动量
long_annual = slope_long * 250  # ✅ 正确
```

**综合得分** = 短期年化 × 1.0 + 长期年化 × 0.5
（不截断，保留真实值）

**v14修复的重大bug**：
1. ❌ 旧版：`long = short`（数据不足250天时，长期=短期，导致得分重复）
2. ❌ 旧版：`(np.exp(slope * 250) - 1)`（指数爆炸，236.9555 → 23695%）
3. ❌ 旧版：`combined = max(-2, min(2, combined))`（截断到[-2,2]，前6名都是2.0）
4. ✅ 修复：`long = 0`（数据不足时长期=0）；去掉exp()；去掉截断

### 4. 禁止行为
- ❌ 禁止使用旧版 `拉普拉斯_盘中监控_v2.py`（无交易记录系统）
- ❌ 禁止使用Backtrader框架（BlakeVer已改用vectorbt）
- ❌ 禁止回测用收盘价判断止损（会低估风险）
- ❌ 禁止邮件用深色背景（必须用亮色主题）
- ❌ 禁止邮件排版偏离三马七星布局

### 5. 新策略回测流程
1. 基于 `blakever_regime_backtest_v4.py` 修改
2. 确保止损逻辑使用最低价
3. 在 `strategy_arena/` 目录创建新文件
4. 测试通过后更新定时任务

---
*本条规则创建于 2026-05-24，优先级：最高*
