# 七星172盘中监控任务提示词

## 任务目标

搭建七星172策略的**盘中自动监控系统**, 在交易日关键时间节点自动计算 ETF 动量排名、判断是否换仓、记录交易并发送邮件报告。

---

## 定时触发规则

### 基础约束

| 项目 | 说明 |
|------|------|
| 交易日 | 每周一至周五（排除法定节假日） |
| 数据源 | 每日开盘前自动更新全部 39 只 ETF 日线数据至最新交易日 |
| 执行脚本 | `reporting/generate_172_report.py`（复用现有报告+交易引擎） |

### 触发时间表

| 时间 | 名称 | 触发目的 |
|------|------|----------|
| **09:30** | 开盘首检 | 开盘时获取前日收盘排名，确认持仓状态，发送晨间简报 |
| **10:30** | 早盘巡检 | 开盘 1 小时后复查排名，捕捉早盘动量异动，有信号则换仓 |
| **11:20** | 午盘预警 | 上午收盘前 10 分钟，动量变化较大的 ETF 触发保护性换仓 |
| **13:10** | ⚡ 核心交易窗口 | **策略主执行点**（与回测引擎对齐）：先卖出旧持仓，再买入新标的 |
| **14:10** | 午后复核 | 核验换仓后排名，防止尾盘反转 |
| **14:50** | 收盘前终检 | 收盘前 10 分钟最终校验，发送全天完整交易报告 |

---

## 每次触发执行流程

```
┌─────────────────────────────────────────────────────┐
│  Step 1  更新 ETF 数据                               │
│          确保 data/storage/stock_data/etf/ 下全部    │
│          39 个 CSV 覆盖至最新交易日                   │
├─────────────────────────────────────────────────────┤
│  Step 2  计算动量排名                                │
│          调用 get_current_rankings()                  │
│          输出: 38 只 ETF 的短期/长期得分 + 过滤状态   │
├─────────────────────────────────────────────────────┤
│  Step 3  交易决策                                    │
│          check_and_execute_trades(ranked)             │
│          - 当前持仓 = 排名#1(不过滤) → 不变           │
│          - 当前持仓 ≠ 排名#1(不过滤) → 换仓           │
│          - 卖出旧持仓+买入新标的 → 写入 xlsx          │
├─────────────────────────────────────────────────────┤
│  Step 4  生成邮件报告                                │
│          按现有 HTML 模板生成完整报告，包含:          │
│          · ETF 动量排名 Top 10（短期/长期得分+涨跌幅）│
│          · 最近 20 条交易记录（含盈亏列）             │
│          · 交易信号摘要                               │
├─────────────────────────────────────────────────────┤
│  Step 5  发送邮件                                    │
│          QQ 邮箱 SMTP → 848786642@qq.com             │
│          主题: 🚀 七星172盘中监控 - {时间标签}        │
└─────────────────────────────────────────────────────┘
```

---

## 关键改造点

> 当前 `generate_172_report.py` 中 `LATEST_DATE` 为硬编码，需改造为**自动获取最新交易日**。

### 1. 动态日期

```python
# 替代硬编码 LATEST_DATE = '2026-05-27'
from datetime import datetime, timedelta

def get_latest_trading_date():
    """自动获取最新交易日（跳过周末）"""
    today = datetime.now()
    # 周六 → 周五, 周日 → 周五
    if today.weekday() == 5:
        today = today - timedelta(days=1)
    elif today.weekday() == 6:
        today = today - timedelta(days=2)
    return today.strftime('%Y-%m-%d')
```

### 2. 盘中时间标签

每次触发时根据当前时间生成不同的邮件主题标签：

| 时间 | 标签 |
|------|------|
| 09:30 | ☀️ 开盘首检 |
| 10:30 | 📈 早盘巡检 |
| 11:20 | ⏰ 午盘预警 |
| 13:10 | ⚡ 核心交易 |
| 14:10 | 🔍 午后复核 |
| 14:50 | 🏁 收盘终检 |

### 3. 交易日判断

非交易日（周末/节假日）**自动跳过**，不触发任何动作：
- 基础判断：`weekday() < 5`（周一到周五）
- 进阶：接入交易日历 API 排除法定假日（如 `akshare.tool_trade_date_hist_sina`）

### 4. 防重复换仓

同一天内如果已在较早时间点换仓，后续触发**不再重复换仓**（仅更新排名和发送邮件）。判断逻辑：

```python
# 检查当天是否已有买卖记录
today_trades = df[df['交易日期'] == today_str]
if len(today_trades) >= 2:  # 已有卖出+买入
    skip_trade = True
```

---

## 定时任务部署方式

### 方案 A: Cron（推荐，简洁可靠）

```bash
# 编辑 crontab
crontab -e

# 添加以下条目（北京时间 CST=UTC+8）
30 1 * * 1-5  cd /workspace/blakever_trade && python3 reporting/generate_172_report.py --tag 开盘首检
30 2 * * 1-5  cd /workspace/blakever_trade && python3 reporting/generate_172_report.py --tag 早盘巡检
20 3 * * 1-5  cd /workspace/blakever_trade && python3 reporting/generate_172_report.py --tag 午盘预警
10 5 * * 1-5  cd /workspace/blakever_trade && python3 reporting/generate_172_report.py --tag 核心交易
10 6 * * 1-5  cd /workspace/blakever_trade && python3 reporting/generate_172_report.py --tag 午后复核
50 6 * * 1-5  cd /workspace/blakever_trade && python3 reporting/generate_172_report.py --tag 收盘终检
```

> 注意：沙箱环境为 UTC 时区，北京时间 = UTC + 8，因此 09:30 CST = 01:30 UTC。

### 方案 B: automation-task-manager（技能调用）

直接使用系统中已有的 `automation-task-manager` 技能创建定时任务，无需手动编辑 crontab。

---

## 邮件报告模板

参考现有模板 `reporting/template/七星172报告模板.md`，内容包含：

- **策略概况**：ETF池 38 只、周期 25 日、佣金 0.02%
- **当前持仓**：代码 + 买入价 + 买入日期
- **交易信号**（发生换仓时显示）
- **ETF 动量排名 Top 10**：排名、名称、代码、综合得分、短期得分、长期得分、价格、涨跌幅、变动
- **最近 20 条交易记录**：日期、方向、ETF、代码、价格、得分、理由、**盈亏**
- **规则说明**：盈利保护 → 成交量放量 → 短期动量<0 → 近3日跌幅>3%

---

## 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 数据未更新 | 先用昨日数据计算排名，邮件中标注"数据截止: YYYY-MM-DD" |
| 全部 ETF 被过滤 | 换仓至防御标的 `sh511880 银华日利` |
| 网络异常/邮件失败 | 记录错误日志，下次触发时重试 |
| 同一交易日多次触发 | 仅首次触发时执行换仓检查，后续仅更新排名+发邮件 |
| xlsx 文件被锁定 | 重试 3 次间隔 2 秒，仍失败则跳过并报警 |

---

## 验证清单

部署完成后逐项确认：

- [ ] 非交易日不触发任何动作
- [ ] 09:30 正常发送晨间排名报告
- [ ] 13:10 换仓逻辑与回测引擎对齐（先卖后买）
- [ ] 换仓后 xlsx 正确追加买入+卖出两条记录
- [ ] 盈亏列数值准确（卖价 − 买价）/ 买价
- [ ] 邮件包含完整排名 + 最近 20 条交易（倒序）
- [ ] 收盘终检包含全天交易汇总

---

*本提示词由七星172策略系统生成，用于指导盘中监控任务的自动化部署。*
